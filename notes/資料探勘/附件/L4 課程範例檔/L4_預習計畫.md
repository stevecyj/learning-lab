# L4 課程範例檔預習計畫

## 這份筆記的目的

這份筆記根據本目錄內的 Notebook、PDF 與資料集，推測老師可能採用的授課順序，並整理上課前適合補齊的 NumPy、Pandas 與 scikit-learn 前置知識。

以下順序不是正式課綱，而是根據三項線索推測：

1. Notebook 儲存的 cell 執行順序。
2. 兩份補充 PDF 的章節大綱。
3. `紅酒資料集預測_part1.ipynb` 與 `part2.ipynb` 刻意留下的練習空格。

## 先照這個順序開檔案

這是本筆記採用的**唯一閱讀順序**。下方的 Session 1～5 也是按照這個順序編排。

```text
1. sklearn複習.ipynb

2. 特徵工程_補充.pdf
3. EDA複習.ipynb

4. 迴歸演算法_補充.pdf
5. 迴歸演算法複習.ipynb

6. 紅酒資料集預測_part1.ipynb
7. 紅酒資料集預測_part2.ipynb
```

其中，PDF 負責解釋觀念，緊接在 PDF 後面的 Notebook 負責示範程式。兩份紅酒 Notebook 是最後的綜合練習。

整體學習主線是：

```text
資料結構複習
→ 資料探索與清理
→ scikit-learn 標準流程
→ 迴歸模型
→ 特徵選擇
→ 紅酒資料綜合練習
```

## 各 Session 的教材與內容

### Session 1：基礎資料操作

**這個 Session 只開一個檔案：**

```text
sklearn複習.ipynb
```

這份 Notebook 雖然叫做「sklearn 複習」，實際內容同時複習了 Pandas、NumPy 和 scikit-learn，應該是整堂課的基礎工具準備。

以下 15 點全部是 `sklearn複習.ipynb` 內部由上往下的內容，**不是 15 個 Session，也不是 15 個檔案**：

1. 從 list、dict 建立 DataFrame。
2. 使用 Pandas 讀取 CSV。
3. 使用 `head()`、`tail()`、`describe()` 觀察資料。
4. 使用布林條件篩選資料。
5. 刪除 row 或 column，並重設 index。
6. 選取多個欄位並輸出 CSV。
7. 使用 `apply()` 與 `lambda` 轉換資料。
8. 建立 NumPy array，觀察 `shape` 並練習二維 indexing。
9. 檢查、刪除與填補缺失值。
10. 將類別資料編碼。
11. 在 DataFrame 與 NumPy array 之間轉換。
12. 使用 `train_test_split` 切分訓練資料與測試資料。
13. 使用 `StandardScaler` 標準化資料。
14. 使用 `MinMaxScaler` 縮放資料。
15. 使用 MSE、R²、accuracy 與 confusion matrix 評估結果。

### Session 2：EDA、缺失值與異常值

**這個 Session 依序開啟兩個檔案：**

```text
1. 特徵工程_補充.pdf
2. EDA複習.ipynb
```

`特徵工程_補充.pdf` 的章節順序是：

1. Data Exploration
2. Feature Cleaning
3. Feature Engineering
4. Feature Selection

`EDA複習.ipynb` 的內容也大致按照這條路線安排。

#### 第一階段：資料探索

1. 讀取糖尿病資料。
2. 檢查欄位資料型別。
3. 取得描述統計。
4. 使用 boxplot 與 histogram 觀察單一變數。
5. 依照類別分組觀察分布。
6. 使用 scatter matrix 觀察變數之間的關係。
7. 使用 correlation matrix 觀察相關性。

#### 第二階段：缺失值處理

Notebook 接著改用 Titanic 資料，示範：

1. 使用 `isnull().sum()` 統計缺失值。
2. 使用 `dropna()` 刪除含缺失值的資料。
3. 使用固定值填補。
4. 使用平均數或中位數填補。
5. 使用分布尾端值填補。

#### 第三階段：異常值處理

異常值偵測方法依序包含：

- 人工指定上下界。
- IQR（四分位距）。
- 平均數與標準差。
- MAD（中位數絕對偏差）。

異常值處理方法包含：

- 改成固定值。
- 封頂或封底（winsorization）。
- 刪除異常資料。
- 使用平均數、中位數或眾數替換。

老師可能先用 PDF 說明「為什麼需要這些處理」，再用 Notebook 示範「如何用程式完成」。

### Session 3：迴歸模型

**這個 Session 依序開啟兩個檔案：**

```text
1. 迴歸演算法_補充.pdf
2. 迴歸演算法複習.ipynb
```

PDF 和 Notebook 都按照以下順序編排：

1. Linear Regression
2. Polynomial Regression
3. Ridge、Lasso、Elastic Net

#### 第一階段：線性迴歸

完整資料流是：

```text
載入資料
→ 分成特徵 X 與目標 y
→ 切分訓練集與測試集
→ 建立 LinearRegression
→ fit
→ predict
→ 使用 MSE 與 R² 評估
```

Notebook 接著從糖尿病資料中抽出單一特徵：

```python
diabetes_X = diabetes.data[:, None, 3]
```

這行同時涉及 NumPy indexing 與維度保持。它選取第 3 號特徵，並用 `None` 保留二維結構，讓結果仍然符合 scikit-learn 對 `X` 的要求。

#### 第二階段：多項式迴歸

Notebook 先用小型矩陣示範 `PolynomialFeatures`，再逐步處理：

- 二次函數。
- 非線性函數。
- 含有雜訊的資料。
- 三次多項式。
- 模型過度擬合。

#### 第三階段：正則化與特徵選擇

最後比較 Lasso、Ridge 與 Elastic Net，並利用 Lasso 產生的零係數篩選特徵：

```python
mask = model.coef_ != 0
diabetes_X = diabetes_X[:, mask]
```

`model.coef_ != 0` 會為每一個特徵產生一個 `True` 或 `False`。第二行使用這組布林值，只保留係數不是零的特徵。

### Session 4：紅酒資料第一次整合練習

**這個 Session 開啟：**

```text
紅酒資料集預測_part1.ipynb
```

這份 Notebook 留有多處空白，應該是課堂練習或作業，不是第一次介紹觀念的教材。

預期完成的流程是：

```text
讀取 winequality-red.csv
→ 定義 X 與 y
→ 建立 PolynomialFeatures
→ 產生 X_poly
→ train_test_split
→ StandardScaler
→ LinearRegression
→ predict
→ 計算 MSE 與 R²
```

這一部分主要測試學生能不能把前面分開學過的步驟重新組合成完整流程。

### Session 5：紅酒資料特徵選擇與建模

**這個 Session 開啟：**

```text
紅酒資料集預測_part2.ipynb
```

Part 2 在 Part 1 前面加入 Lasso 特徵選擇：

```text
讀取紅酒資料
→ 分離 X 與 y
→ 訓練 Lasso
→ 找出非零係數
→ 使用布林 mask 保留特徵
→ 建立多項式特徵
→ 切分資料
→ 標準化
→ 建立線性迴歸模型
→ 評估模型
```

核心程式概念是：

```python
mask = model.coef_ != 0
```

若 `X` 是 NumPy array，可以使用：

```python
X = X[:, mask]
```

若 `X` 是 Pandas DataFrame，較明確的寫法是：

```python
X = X.loc[:, mask]
```

這裡的 `:` 代表保留所有 row，`mask` 則決定保留哪些 column。

## 最值得先補的前置知識

不需要先把 NumPy 和 Pandas 的所有功能讀完。這堂課最依賴的是 shape、indexing、axis，以及 scikit-learn 的固定資料流程。

### 一、先看懂 shape

NumPy 陣列的 `shape` 用來描述每一維有多少個位置：

```text
一維：(元素數量,)
二維：(row 數量, column 數量)
三維：(第 0 維, 第 1 維, 第 2 維)
```

例如：

```python
X.shape == (100, 11)
```

代表 `X` 有 100 筆資料，每筆資料有 11 個特徵。

在 scikit-learn 中，常見的資料形狀是：

```text
X：二維，(樣本數, 特徵數)
y：一維，(樣本數,)
```

這是整堂課最重要的資料契約。每執行一次選取、轉換或切分，都應該確認資料是否仍符合這個格式。

### 二、把 indexing 分成三類學

不要把所有索引語法混在一起背。

#### 1. NumPy 位置索引

```python
array[2]
array[2, 3]
array[:, 3]
array[:, mask]
```

#### 2. Pandas 標籤索引

```python
df['Age']
df[['Age', 'Fare']]
df.loc[rows, columns]
```

#### 3. 條件篩選

```python
df[df['Fare'] > 300]
df.loc[df['Fare'] > 300, 'Fare']
```

每次看到 indexing，都先問：

1. 現在選的是 row 還是 column？
2. 選取後的結果是一維還是二維？

例如：

```python
df['Age']       # Series，一維
df[['Age']]     # DataFrame，二維
```

兩者取得的都是 `Age`，但結果的資料結構與 shape 不同。這個差異會影響資料能不能直接交給 scikit-learn。

### 三、不要只用橫向、縱向背 axis

比較穩定的理解方式是：

> `axis=n` 表示操作沿著第 n 維進行。聚合操作通常會讓該維消失。

假設：

```python
a.shape == (3, 4)
```

代表 `a` 有 3 rows、4 columns。

執行：

```python
a.mean(axis=0)
```

程式會把第 0 維的 3 rows 聚合掉，每個 column 留下一個平均數，因此結果的 shape 是：

```text
(4,)
```

執行：

```python
a.mean(axis=1)
```

程式會把第 1 維的 4 columns 聚合掉，每一個 row 留下一個平均數，因此結果的 shape 是：

```text
(3,)
```

不過，不同函式對 axis 執行的動作不同，所以必須把「函式動作」和 `axis` 一起讀。

```python
df.drop('Season', axis=1)
```

這裡是從 column 軸刪除 `Season`。

```python
pd.concat([left, right], axis=1)
```

這裡是沿著 column 軸把資料排在彼此旁邊。

可以先這樣區分：

- `mean(axis=0)`：聚合第 0 維。
- `drop(..., axis=1)`：從 column 軸刪除資料。
- `concat(..., axis=1)`：沿 column 軸增加或對齊資料。

### 四、建立 scikit-learn 固定流水線

先記住資料的生命週期，不要一開始就背每個模型的參數：

```text
原始 DataFrame
→ 分離 X 與 y
→ train/test split
→ 用 X_train fit 前處理器
→ transform X_train
→ 用同一個前處理器 transform X_test
→ model.fit(X_train, y_train)
→ model.predict(X_test)
→ metric(y_test, y_pred)
```

關鍵規則是：

> 測試資料只能套用訓練資料算出的轉換規則，不能用測試資料重新 `fit()`。

正確流程如下：

```python
scaler = StandardScaler().fit(X_train)

X_train = scaler.transform(X_train)
X_test = scaler.transform(X_test)
```

`fit(X_train)` 會從訓練資料算出平均數和標準差。之後兩份資料必須使用同一組數值轉換，才不會讓測試資料提前影響模型流程。

## 實際預習行程

這裡的行程與前面的 Session 1～5 完全對應，不另外建立第二套順序。

### Session 1：完成基礎資料操作

**開啟檔案：** `sklearn複習.ipynb`

**執行方式：** 從第一個 cell 開始依序執行到最後，不要跳著跑。

這次先掌握：

- DataFrame 的 row 與 column。
- NumPy 的 `shape` 與二維 indexing。
- 缺失值與類別編碼。
- `train_test_split`、scaler 與評估指標。

每次選取或轉換資料後，都印出 `shape`，確認哪一維發生改變。

**完成條件：** 能說明 `X`、`y`、`X_train`、`X_test` 分別儲存什麼。

### Session 2：完成 EDA 與資料清理

**依序開啟：**

```text
1. 特徵工程_補充.pdf
2. EDA複習.ipynb
```

先讀 PDF 的 Data Exploration 與 Feature Cleaning，再執行 Notebook 中對應的程式。

這次先掌握：

- 單一變數的分布與多變數之間的關係。
- 缺失值與異常值的差別。
- 偵測異常值與處理異常值的差別。
- 布林 mask 如何標記需要處理的 row。

不必先背 IQR、MAD 的完整公式，先看懂它們最後如何產生 `True`／`False`。

**完成條件：** 能從讀取 CSV 開始，完成檢查資料、找出缺失值及標記異常值。

### Session 3：完成迴歸模型

**依序開啟：**

```text
1. 迴歸演算法_補充.pdf
2. 迴歸演算法複習.ipynb
```

先讀 PDF 中對應的模型概念，再依序執行 Notebook 的 Linear Regression、Polynomial Regression、Lasso、Ridge 與 Elastic Net。

執行時特別觀察：

```python
print(X.shape)
print(X_poly.shape)
print(model.coef_.shape)
print(mask.shape)
```

**完成條件：** 能解釋 `PolynomialFeatures` 為什麼會增加 column，以及 Lasso 的係數如何變成特徵 mask。

### Session 4：完成紅酒 Part 1

**開啟檔案：** `紅酒資料集預測_part1.ipynb`

依照 Notebook 註解補上空白程式：

```text
讀取資料
→ 分離 X 與 y
→ 建立多項式特徵
→ 切分訓練集與測試集
→ 標準化
→ 訓練模型
→ 預測
→ 評估
```

**完成條件：** Notebook 可以從頭執行到尾，並輸出 MSE 與 R²。

### Session 5：完成紅酒 Part 2

**開啟檔案：** `紅酒資料集預測_part2.ipynb`

在 Part 1 的流程前面加入 Lasso 特徵選擇，確認：

- `mask` 的長度和原始 `X` 的 column 數相同。
- 篩選後的 `X` column 數量變少或維持不變。
- 訓練集與測試集使用同一個 scaler。
- `y_test` 和 `y_pred` 長度一致。

**完成條件：** 能從 Lasso 係數建立 mask，選完特徵後完成模型訓練與評估。

## 可以暫時不預習的內容

下列內容不是完成紅酒練習的必要門檻：

- 手算 normal equation。
- 完整推導 gradient descent。
- NumPy broadcasting 的所有規則。
- 三維以上陣列的複雜 axis 操作。
- 所有缺失值機制的統計檢定。
- Wrapper method 的完整實作。
- Elastic Net 的完整數學推導。

目前最重要的是，每看到一行資料處理程式，都能回答：

```text
資料現在是什麼形狀？
→ 這行選取了哪些資料？
→ 這個操作改變了哪一維？
→ 下一個 scikit-learn 步驟期待什麼形狀？
```

只要養成追蹤資料形狀和資料流的習慣，NumPy 與 Pandas 就不再是一堆互不相關的語法，而會變成一條可以逐步檢查的資料變形流程。

## 上課前最低檢查清單

- [ ] 我看得懂 `(100, 11)` 代表 100 筆資料、11 個特徵。
- [ ] 我知道 `df['Age']` 和 `df[['Age']]` 的結果維度不同。
- [ ] 我可以分辨 row selection、column selection 和 boolean selection。
- [ ] 我能先猜 `mean(axis=0)` 和 `mean(axis=1)` 的輸出 shape。
- [ ] 我知道 `X`、`y`、`X_train`、`X_test` 分別儲存什麼。
- [ ] 我知道 `fit()`、`transform()` 和 `predict()` 的責任不同。
- [ ] 我知道 scaler 只能在訓練資料上 `fit()`。
- [ ] 我能說明布林 mask 如何篩選特徵。
- [ ] 我能按順序寫出一條基本的模型訓練與評估流程。
