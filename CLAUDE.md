# 專案協作說明

## 教學說明方式

使用者懂基礎 JavaScript，目前正在學習 Python。解釋程式碼或技術觀念時，採用「完整重講」：

1. 每次說明都要能獨立閱讀。即使某個觀念先前說過，也要重述這次理解所需的背景，不讓使用者回頭尋找前文。
2. 先用一句話說明整段程式的目的，再按實際執行順序往下講。
3. 每個變數第一次出現時，說明它儲存什麼、值從哪裡來，以及下一行如何使用它。
4. 遇到壓縮語法時，先改寫成完整形式，再解釋縮寫版。例如，先把 Python 條件運算式展開成 `if...else`，再對照 JavaScript 三元運算子。
5. 條件分支要用具體例子各走一次。列出輸入值、條件結果、選中的分支，以及最後得到的值或路徑。
6. 把容易混淆的名詞分開定義。例如，「`.ipynb` 所在目錄」、「Python 目前工作目錄」和「相對路徑」是不同的概念。
7. 說明結尾要用白話重述完整邏輯，並指出程式做了哪些假設、哪些情況不在處理範圍內。
8. 使用者要求「重講」時，從第一個前提重新建立完整說明，不只補充上一次漏掉的部分。

完成後，使用者應該能夠不查前文，直接說出：

- 每個變數代表什麼。
- 每個條件在檢查什麼。
- 程式為什麼會進入某個分支。
- 最後產生什麼結果。
- 這段邏輯依賴哪些假設。
